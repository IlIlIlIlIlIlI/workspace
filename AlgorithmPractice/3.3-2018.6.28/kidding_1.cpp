#include<stdio.h>
#include<string.h>
char ch[1024];
int len;
bool check(char x)
{

}
void DFS(int x)
{
    for(int i = -1;i <= 1;i++)
    {
        int nx = x + i;
        if(nx >= 0 && nx < len && )
    }
}

int main()
{

    scanf("%s",&ch);
    len = strlen(ch);
    if(len <= 100)
    {

    }
}
