#include<stdio.h>
#define maxn 1024
char tem1,now;
char compare(int b[])
{
    int now = 0;
    for(int i = 0;i<n;i++)
    {
        tem1 += a[i];
    }
    for(int i = (++now);i<n;i++)
    {
        tem2 += a[i];
    }
    if(strcmp(tem,now) > 0))
    {
        return tem;
    }
    else if(strcmp(tem,now) < 0)
        {
            tem = now;
        }
}
int main()
{
    //n<=100
    int n;
    scanf("%d",&n);
    //每个元素不超过1000
    int a[maxn];
    for(int i = 0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }

}
