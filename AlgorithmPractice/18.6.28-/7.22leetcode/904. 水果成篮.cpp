
class Solution {
public:
    int totalFruit(vector<int>& tree) {
        if(tree.size() < 3)
        {
            return tree.size();
        }
        else
        {

        }
    }
private:
    int helper(vector<int>& tree, int index)
    {
        queue<int> q;
        int sum = 2, f = 0, n1 = tree[index], n2 = tree[index+1];
        for(int i = index; i < tree.size(); i++)
        {
            if(n1 < 0 && )
        }
    }
};
